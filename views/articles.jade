extends layout

block content
  h1 Articles
  ol
      each doc, i in docs
          li
              ul
                  li
                      h4 #{doc.title}
                  li
                      h4 #{doc.description}
                  li
                      h4 #{doc.author}
